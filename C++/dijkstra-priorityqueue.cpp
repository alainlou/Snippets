#include <bits/stdc++.h>
using namespace std;

int main(){
	int n,m;
	cin >> n >> m;
	source = 1
	
	pair<int, int> costs[n];
	for(int i = 0; i < n; i++){
		costs[i] = make_pair(INT_MAX, undefined);
	}
	
	costs[source] = make_pair(
	return 0;
}

